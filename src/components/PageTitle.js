import React from "react";

const PageTitle = ({ title }) => {
  return (
    <div>
      <div className="page-title">
        <h3>{title}</h3>
      </div>
    </div>
  );
};

export default PageTitle;
